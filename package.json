{
  "name": "1346.lol",
  "version": "1.0.0",
  "description": "Ultraviolet proxy site",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "echo \"No build steps required\"",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --detectOpenHandles",
    "test:ci": "NODE_OPTIONS=--experimental-vm-modules jest --ci --detectOpenHandles"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@tomphttp/bare-server-node": "^2.x",
    "express": "^5.x"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}